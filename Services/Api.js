
const create = (baseURL = 'https://example/') => {
  // TODO Create and configure a default fetch method.
  const getSpeakers = () => {}
  const getNearby = () => {}

  return {
    getSpeakers,
    getNearby
  }
}

export default {
  create
}